# Сервис назначения ревьюеров для Pull Request’ов

## Техническое задание
- 📄 [Техническое задание](https://github.com/avito-tech/tech-internship/blob/main/Tech%20Internships/Backend/Backend-trainee-assignment-autumn-2025/Backend-trainee-assignment-autumn-2025.md)

## Технологии
- **Язык программирования и фреймворк:**  
  🐍 Python, Django REST Framework (DRF) — построение REST API.

- **База данных:**  
  🗄️ PostgreSQL — реляционная СУБД для хранения данных.

- **Контейнеризация и деплой:**  
  🐳 Docker (Dockerfile, docker-compose) — управление средой и запуск.

- **CI/CD:**  
  ⚙️ GitHub Actions — автоматизация сборки, тестирования и деплоя.

- **Документация API:**  
  📄 OpenAPI спецификация и Redoc — визуализация и тестирование API.

## Схема БД
![Схема Схема БД](путь_к_изображению.png)

## Эндпоинты API
> 🗃️ Доступ к полной спецификации API возможен по адресу:  
> [http://localhost:8080/redoc/](http://localhost:8080/redoc/)

- **POST** `/api/team/add/` — Создать команду с участниками
- **GET** `/api/team/get/` — Получить команду с участниками
- **POST** `/api/team/deactivate/` — Деактивировать команду

- **POST** `/api/users/setIsActive/` — Установить флаг активности пользователя
- **POST** `/api/users/changeTeam/` — Сменить команду пользователя, если нет открытых PR
- **GET** `/api/users/getReview/` — Получить PR, где пользователь назначен ревьювером

- **POST** `/api/pullRequest/create/` — Создать PR и назначить ревьюверов
- **POST** `/api/pullRequest/reassign/` — Переназначить ревьювера
- **POST** `/api/pullRequest/merge/` — Пометить PR как MERGED

- **GET** `/api/statisticsUser/` — Получить статистику пользователей
- **GET** `/api/statisticsPR/` — Получить статистику pull requests

## Запуск
🚀 Для запуска проекта выполните следующие шаги:

1. 📥 Клонируйте репозиторий:
```bash
git clone https://github.com/MishaAstanin/pull-request-service.git
```

2. 📁 Перейдите в корневую директорию сервиса pull-request-service:
```bash
cd pull-request-service
```

3. 🐳 Запустите проект с помощью Docker Compose:
```bash
docker compose up
```

После запуска сервис будет доступен локально на порту **8080**

---

**Требования:**

- 🐋 Установленный Docker.
- 🌐 Доступ к интернету для загрузки необходимых образов Docker.

## Отклонения от условий ТЗ
- При создании объектов не требуется указывать их `id` в запросе — идентификаторы присваиваются автоматически сервером.  
- Это упрощает работу с API, снижая вероятность ошибок и дублирования, а также позволяет централизованно управлять уникальностью объектов.

## Дополнения к ТЗ
- 🌟 **Смена команды пользователя:** реализована возможность смены команды, если нет pull-requests со статусом `OPEN`, на которые пользователь назначен ревьюером.
- 🔄 **Переназначение ревьюеров:** при деактивации пользователя pull requests, на которые он назначен ревьюером, автоматически переназначаются другим участникам команды, если это возможно.
- 🚫 **Деактивация команды:** реализован функционал деактивации всех участников команды `is_active=false`.
- 📊 **Статистика:** добавлены API-эндпоинты для получения статистики пользователей и pull requests.
- ✅ **Интеграционное тестирование:** полноценно покрыт критический функционал.
- 📄 **Спецификация API:** написана и поддерживается в актуальном состоянии.
- ⚙️ **CI/CD:** настроена автоматизация проверок — при каждом коммите запускаются линтер `ruff` и тесты, после чего обновлённые Docker-образы автоматически собираются и публикуются на Docker Hub.

## Дальнейшее развитие проекта
Планируется добавить аутентификацию по `JWT` токену и ролевую модель пользователей, чтобы обеспечить безопасный доступ и разграничение прав в системе.

## Связь и вклад в проект

Буду рад видеть ваши предложения и вклад в развитие Проекта. Пожалуйста, обращайтесь через систему pull request или создайте issue с описанием вашего предложения.